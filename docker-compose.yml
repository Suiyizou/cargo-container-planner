services:
  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    image: cargo-planner-frontend:0.1.0
    container_name: cargo-planner-frontend
    ports:
      - "${FRONTEND_PORT:-80}:80"
    restart: unless-stopped
